{
  "name": "cf_ai_cloudflare_chat_memory",
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "build": "esbuild src/worker.ts --bundle --format=esm --outfile=dist/worker.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250923.0",
    "esbuild": "^0.23.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.40.0"
  },
  "dependencies": {
    "itty-router": "^4.2.2"
  }
}
